
const creds = {
    host     : 'host',
    user     : 'user',
    password : 'password',
    database : 'database',
    port: 3306
}

// Node.js - when i require this file, this will give it
module.exports = creds;
